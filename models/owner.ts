export class Owner {
  username: string
  url: string
  avatar: string

  constructor(username: string, url: string, avatar: string) {
    this.username = username
    this.url = url
    this.avatar = avatar
  }
}
